services:
  backend:
    build: .
    container_name: securehr-backend
    working_dir: /app/backend
    command: node server.js
    ports:
      - "5001:5000"
    environment:
      - PORT=5000
      - JWT_SECRET=securehr_secret_key

  frontend:
    build: .
    container_name: securehr-frontend
    working_dir: /app/frontend
    command: npm start
    ports:
      - "3000:3000"
    environment:
      - CHOKIDAR_USEPOLLING=true
    depends_on:
      - backend